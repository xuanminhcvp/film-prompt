# 10 — Nhịp không thoại (Nhịp lặng / B-roll)

> **File này trả lời:** trọn đời một shot không thoại — khi nào cần, đặt ở đâu, đặt mã gì, prompt video viết sao, nhạc ra sao.
> **Mở khi:** rà mối nối giữa hai scene, hoặc khi thấy chuỗi thoại quá dài cần kéo giãn.
> **Không chứa:** shot có thoại (→ `7-bang-shot.md`) ·
>   cách viết prompt Suno (→ `12-nhac-suno.md`) ·
>   form prompt video của shot thoại (→ `11-prompt-video.md`)

> ⛔ **LUẬT TUYỆT ĐỐI CHO HOOK**: Trong skill `skills-hook`, HOOK CÓ NHỊP ĐỘ DỒN DẬP VÀ TUYỆT ĐỐI KHÔNG DÙNG NHỊP LẶNG (0 nhịp lặng). Mọi shot trong Hook đều là shot thoại hoặc hành động dồn dập có thoại/nội dung trực tiếp. Không sinh ra bất kỳ shot nhịp lặng nào trong `sf-board.json` của dự án Hook.

## 1. Đặt ở đâu
*(Không áp dụng cho Hook — Hook không có nhịp lặng).*

## 2. Phân loại
*(Không áp dụng cho Hook — Hook không có nhịp lặng).*

## 3. Định mức
- **HOOK: 0% nhịp lặng (TUYỆT ĐỐI KHÔNG DÙNG NHỊP LẶNG).**

## 4. Đặt tên & đặt mã
*(Không áp dụng cho Hook — Hook không có nhịp lặng).*

## 5. Neo bối cảnh
*(Không áp dụng cho Hook — Hook không có nhịp lặng).*

## 6. Prompt video của nhịp
*(Không áp dụng cho Hook — Hook không có nhịp lặng).*

## 7. Nhạc — bắt buộc
*(Không áp dụng cho Hook — Hook không có nhịp lặng).*

## 8. Kiểm tra
- [ ] **Hook:** 0 nhịp lặng (tuyệt đối không chèn bất kỳ shot nhịp lặng / `-B<n>` nào).
