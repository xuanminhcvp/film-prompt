# Hướng dẫn AI trong repo

Luôn trả lời người dùng bằng tiếng Việt.

Kho này chỉ phục vụ việc viết prompt cho `sf-board.json`. Luật đầy đủ nằm ở
[CLAUDE.md](CLAUDE.md); đọc file đó trước khi làm bất cứ việc gì.

Trước khi viết hoặc sửa prompt, đọc `.claude/skills/skills-film/SKILL.md` rồi mở
đúng file `references/` của bước đang làm. Khung có trẻ em: bắt buộc mở
`references/LUAT-an-toan.md`.

Không tự sửa skill, không sửa kịch bản gốc, không đụng ảnh/video đang dùng.
Không pull/push/sync remote nếu người dùng chưa cho phép chính xác.
